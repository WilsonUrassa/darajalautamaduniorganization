'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
import {supabase} from '../../lib/supabaseClient';
type Leader={id:string;name:string;role:string;bio:string|null;photo_url:string}; type Content={id:string;section:string;title:string;body:string|null;metric_value:string|null;metric_label:string|null;image_url:string|null;document_url:string|null;document_name:string|null};
export default function AboutPage(){const [leaders,setLeaders]=useState<Leader[]>([]);const [content,setContent]=useState<Content[]>([]);useEffect(()=>{Promise.all([supabase.from('organization_leaders').select('*').eq('published',true).order('sort_order'),supabase.from('organization_content').select('*').eq('published',true).order('sort_order')]).then(([l,c])=>{setLeaders(l.data||[]);setContent(c.data||[])})},[]);const section=(s:string)=>content.filter(x=>x.section===s);return <main><section className="hero"><div className="container"><p className="eyebrow">About Daraja la Utamaduni</p><h1>Know the organization<br/><em>behind the work.</em></h1><p className="hero-lead">Our mission, leadership, programs, impact and accountability—presented clearly for communities, partners and donors.</p></div></section><section className="section"><div className="container two-col"><div><p className="eyebrow">Who we are</p><h2>{section('identity')[0]?.title||'A bridge between people, culture and opportunity.'}</h2></div><div className="intro-text">{section('identity')[0]?.body?<p>{section('identity')[0].body}</p>:<><p>Daraja la Utamaduni Organization is a community-focused organization committed to sustainable development while respecting the knowledge, culture and aspirations of the people we serve.</p><p>Verified mission, vision and background information can be published from the admin dashboard.</p></>}</div></div></section><section className="section section-soft"><div className="container"><div className="section-heading"><div><p className="eyebrow">Leadership</p><h2>People guiding the organization.</h2></div><p>Meet the leaders responsible for guiding Daraja la Utamaduni and its work.</p></div><div className="leader-grid">{leaders.length?leaders.map(l=><article className="leader-card" key={l.id}><img src={l.photo_url} alt={l.name}/><div><span>{l.role}</span><h3>{l.name}</h3>{l.bio&&<p>{l.bio}</p>}</div></article>):<div className="empty-state"><strong>Leadership profiles will appear here.</strong><p>Photos, names, roles and biographies can be uploaded from the admin dashboard.</p></div>}</div></div></section><section className="section"><div className="container"><div className="section-heading"><div><p className="eyebrow">Our impact</p><h2>Evidence of meaningful change.</h2></div><p>Publish verified results, reach, outcomes and stories here as the organization approves them.</p></div><div className="impact-items impact-light">{section('impact').map(x=><div key={x.id}><strong>{x.metric_value||'—'}</strong><span>{x.metric_label||x.title}</span></div>)}</div></div></section><section className="section section-soft"><div className="container two-col"><div><p className="eyebrow">Accountability</p><h2>Transparency that partners can review.</h2></div><div className="intro-text">{section('accountability').map(x=><div key={x.id}><h3>{x.title}</h3><p>{x.body}</p>{x.document_url&&<a className="text-link" href={x.document_url} target="_blank" rel="noreferrer">{x.document_name||'View document'} →</a>}</div>)}{!section('accountability').length&&<p>Annual reports, policies, safeguarding information, financial documents and other approved materials can be published from the admin dashboard.</p>}</div></div></section><section className="section"><div className="container partner-box"><div><p className="eyebrow">Work with us</p><h2>Ready to learn more?</h2></div><div className="partner-actions"><Link href="/projects" className="button button-primary">View programs</Link><Link href="/impact" className="button button-outline">Our impact</Link></div></div></section></main>}
